{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "Sample chat app ",
  "repository": "https://github.com/elkhoudiry/chat-app-dev.git",
  "author": "elkhoudiry <ahmedelkhodery2@Gmail.com>",
  "license": "MIT",
  "scripts": {
    "dev": "lerna run dev --stream",
    "build": "yarn lerna bootstrap && lerna run build --stream",
    "start": "lerna run start --stream --scope=server"
  },
  "devDependencies": {
    "@types/node": "^16.9.1",
    "typescript": "^4.4.3",
    "yarn": "^1.22.11"
  },
  "dependencies": {
    "lerna": "^4.0.0"
  }
}
